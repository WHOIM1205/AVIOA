# Copy to `.env` (or `.env.local`) to override the backend URL.
# Must be prefixed with VITE_ for Vite to expose it to the browser.
# If unset, the app defaults to http://localhost:8000 (see src/config.js).
VITE_API_BASE_URL=http://localhost:8000
